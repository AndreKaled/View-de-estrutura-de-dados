# View-de-estrutura-de-dados

## Como testar?
Rode
```bash
make dev_docker
```

em seguida, rode
```bash
python3 api/app.py
```

Alterações de `app.py` são as saídas json capturadas, existem mains separados para cada estrutura, basta chamar corretamente em `app.py`.